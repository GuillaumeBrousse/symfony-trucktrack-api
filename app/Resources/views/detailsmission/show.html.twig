{% extends 'base.html.twig' %}

{% block body %}
    <h1>Detailsmission</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detailsMission.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detailsmission_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detailsmission_edit', { 'id': detailsMission.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
